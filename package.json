{
  "name": "payme",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "stripe": "^8.15.0"
  }
}
